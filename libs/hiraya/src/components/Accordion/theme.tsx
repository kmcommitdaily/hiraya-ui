import PermIdentityIcon from '@mui/icons-material/PermIdentity';

const leadingItem = {
  number: (index: number) => index + 1,
  icon: <PermIdentityIcon />,
};

export const primaryBg = 'rgba(0, 0, 0, 0.323)';
export const secondaryBg = '#e0e7efc8';

export { leadingItem };
